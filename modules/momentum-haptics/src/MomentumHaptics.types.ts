/** Intensity of an impact haptic — maps directly to UIImpactFeedbackGenerator.FeedbackStyle on iOS. */
export type ImpactStyle = 'light' | 'medium' | 'heavy';

/** Semantic feedback type — maps to UINotificationFeedbackGenerator.FeedbackType on iOS. */
export type NotificationType = 'success' | 'warning' | 'error';
