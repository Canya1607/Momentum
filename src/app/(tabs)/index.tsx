import { View, Text } from 'react-native';

export default function HabitListScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Habit List — placeholder</Text>
    </View>
  );
}
