import { View, Text } from 'react-native';

export default function HabitEditScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Add / Edit Habit — placeholder</Text>
    </View>
  );
}
